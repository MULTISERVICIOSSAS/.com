{
  "name": "multiservicios-cloudflare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply multiservicios --local",
    "db:migrate:remote": "wrangler d1 migrations apply multiservicios --remote",
    "test": "node --test tests/cloudflare-*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
